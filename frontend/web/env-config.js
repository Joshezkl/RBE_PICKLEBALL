// Runtime configuration loaded before Flutter starts.
// Vercel/CI overwrites this file at build time via scripts/generate-env-config.mjs.
// Empty values fall back to --dart-define flags, then local dev defaults.
window.__RBE_CONFIG__ = window.__RBE_CONFIG__ || {
  apiBaseUrl: '',
  wsHost: '',
  wsScheme: '',
  wsKey: '',
};
